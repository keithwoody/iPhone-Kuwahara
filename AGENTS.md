# iPhone-Kuwahara

## Agent skills

### Issue tracker

Issues and external PRs live in GitHub Issues (`keithwoody/iPhone-Kuwahara`); external PRs are also a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical label strings (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — one `CONTEXT.md` at the repo root + `docs/adr/` for architectural decisions. See `docs/agents/domain.md`.
